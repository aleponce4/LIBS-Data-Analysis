# LIBS Spectroscopy Workbench runtime dependencies
# (the installable package is named prolibspector-public)
numpy>=1.22.0
scipy>=1.8.0
pandas>=1.4.0
matplotlib>=3.5.0
sv-ttk>=2.5.0
pillow>=9.0.0
markdown>=3.4.0
tkhtmlview>=0.1.1
textalloc>=0.0.4
statsmodels>=0.13.0

# Optional extras (guarded imports; install for wavelet denoising and
# Parquet map exports). Equivalent to: pip install -e .[full]
# PyWavelets>=1.3.0
# pyarrow>=8.0.0
